import GlassCard from "../../../shared/ui/GlassCard";
export default function DiaryCard({ entry }) { return <GlassCard className="diary-card" hover={false}><small>{entry.createdAt ? new Date(entry.createdAt).toLocaleDateString("pt-BR") : "Lembrança guardada"}</small><h3>{entry.title || "Uma reflexão guardada"}</h3>{entry.content && <p>{entry.content}</p>}{Array.isArray(entry.tags) && entry.tags.length > 0 && <span>{entry.tags.join(" · ")}</span>}{entry.favorite && <strong>Guardado com carinho</strong>}</GlassCard>; }
